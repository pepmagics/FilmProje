<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Takip Listem</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body>
    <div class="container">
        <h1 class="my-4">Takip Listem</h1>
        <div class="row">
            <% movies.forEach(movie => { %>
            <div class="col-md-3 mb-4">
                <div class="card">
                    <% if (movie.poster_path) { %>
                    <img src="https://image.tmdb.org/t/p/w500/<%= movie.poster_path %>"
                        alt="<%= movie.title %> Poster" class="card-img-top">
                    <% } else { %>
                    <img src="no-image.png" alt="Varsayılan Resim" class="card-img-top">
                    <% } %>
                    <div class="card-body">
                        <h5 class="card-title"><%= movie.title %></h5>
                        <a href="/movie/<%= movie.id %>" class="btn btn-primary">Detaylar</a>
                    </div>
                </div>
            </div>
            <% }); %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>

</html>
